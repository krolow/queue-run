{
  "name": "queue-run",
  "version": "0.2.0",
  "description": "👋 APIs and back-end functions for JAMstack apps (queues, pub/sub, cron, etc)",
  "main": "src/index.js",
  "types": "types/index.d.ts",
  "scripts": {
    "build": "yarn build:queue-run && yarn build:runtime && yarn build:builder && yarn build:cli",
    "build:queue-run": "yarn workspace queue-run build",
    "build:runtime": "yarn workspace @queue-run/runtime-lambda build",
    "build:builder": "yarn workspace @queue-run/builder build",
    "build:cli": "yarn workspace @queue-run/cli build",
    "lint": "lerna run lint"
  },
  "author": "Assaf Arkin <assaf@labnotes.org> (https://labnotes.org/)",
  "homepage": "",
  "license": "MIT",
  "repository": "github:assaf/queue-run.git",
  "private": true,
  "devDependencies": {
    "@tsconfig/node14": "^1.0.1",
    "@typescript-eslint/eslint-plugin": "^5.8.1",
    "@typescript-eslint/parser": "^5.8.1",
    "dts-cli": "^1.1.3",
    "eslint": "8.5.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.25.3",
    "eslint-plugin-prettier": "^4.0.0",
    "eslint-plugin-promise": "^6.0.0",
    "eslint-plugin-sonarjs": "^0.11.0",
    "eslint-plugin-typescript": "^0.14.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
