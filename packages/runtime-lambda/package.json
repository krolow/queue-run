{
  "name": "@queue-run/runtime-lambda",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "build": "dts build",
    "lint": "dts lint src",
    "prepare": "yarn build",
    "watch": "dts watch"
  },
  "files": [
    "dist",
    "src",
    "types"
  ],
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.45.0",
    "queue-run": "^0.1.3",
    "tiny-invariant": "^1.2.0"
  },
  "devDependencies": {
    "dts-cli": "^1.1.3"
  },
  "engines": {
    "node": "14.x"
  }
}
