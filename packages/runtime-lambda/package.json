{
  "name": "queue-run-lambda",
  "version": "0.3.0",
  "main": "dist/index.js",
  "type": "module",
  "directories": {
    "lib": "dist"
  },
  "scripts": {
    "build": "rm -rf dist && yarn build:bundle",
    "build:bundle": "yarn esbuild src/index.ts --bundle --platform=node --target=node14 --outfile=dist/index.js --external:queue-run --format=esm",
    "lint": "yarn eslint src/**/*.ts",
    "prepare": "yarn build",
    "watch": "yarn build:bundle --watch"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.45.0",
    "queue-run": "^0.3.0",
    "tiny-invariant": "^1.2.0"
  },
  "engines": {
    "node": "14.x"
  },
  "gitHead": "cb2455561449ed3629e679e3656bb72e746096e3"
}
