{
  "name": "@queue-run/gateway",
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "dts build --target node --format cjs",
    "lint": "dts lint src",
    "prepare": "yarn build",
    "watch": "dts watch"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.44.0",
    "@aws-sdk/client-lambda": "^3.43.0",
    "ms": "^2.1.3",
    "node-abort-controller": "^3.0.1",
    "tiny-invariant": "^1.2.0"
  },
  "devDependencies": {
    "dts-cli": "^0.20.0"
  },
  "engines": {
    "node": "14.x"
  }
}
