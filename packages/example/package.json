{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "queue-run build",
    "dev": "queue-run dev",
    "lint": "eslint **/*.{ts,tsx}"
  },
  "peerDependencies": {
    "queue-run": "^0.1.4"
  },
  "dependencies": {
    "ow": "^0.28.1"
  },
  "engines": {
    "node": "14.x"
  }
}
