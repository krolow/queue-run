{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "queue-run build",
    "dev": "queue-run dev",
    "deploy": "queue-run deploy",
    "lint": "eslint **/*.{ts,tsx}"
  },
  "peerDependencies": {
    "queue-run": "^3.0.0"
  },
  "engines": {
    "node": "14.x"
  },
  "version": "0.4.0"
}
